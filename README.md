# Centred

Flutter app for completing Person-Centred Planning (PCP) tools.

See `CLAUDE.md` for architecture, design system, and project conventions.
`design-reference/` holds the original Nuxt/Vue prototype (UX/content
reference only, not the shipping product).

## Running locally

```bash
flutter pub get
flutter run
```
